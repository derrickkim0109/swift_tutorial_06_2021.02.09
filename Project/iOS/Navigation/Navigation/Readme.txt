1. Tab Bar 처럼 Embeded in -> Navigation Bar 선택해서 만들어 낸다
2. ViewController 하나 만들었다 (Command + Shift + L 안에서 검색함.)
3. Bar ButtonItem 쓴다. => Edit으로 수정
4. Navigation 화면 --> Segue Connect --> New ViewController에 (Show로)
5. 저 연결 Segue 이름을 단다(editBarButton)
6. 수정버튼도 동일
7. New File--> Cocoa Touch Class 선택--> super : UIViewController 선택 --> Create
8. 수정 화면에서 ViewController 선택후 --> identify에서 --> Class를 UIViewController로 변경해서
    Cocoa Class연결

*** Edit , 수정  두버튼은 넘기는 거라 액션부분에 안들어간다.
